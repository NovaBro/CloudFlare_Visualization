## Instrustions to Run Locally

**Prerequisites:**  Node.js


1. Remove existing dependencies (node_modules folder) - only the first time
   `rm -rf node_modules/`
2. Install dependencies:
   `npm install`
3. Run the app:
   `npm run dev`
